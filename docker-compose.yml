version: '3.8'

services:
  splitwatch_bot:
    build: .
    container_name: splitwatch_bot
    env_file:
      - .env
    restart: unless-stopped
    # Optional: If your MQTT broker is also in Docker Compose, uncomment and configure:
    # depends_on:
    #   - mqtt_broker
    # networks:
    #   - my_network

# Optional: Define networks if needed
# networks:
#   my_network:
#     driver: bridge

# Optional: Define MQTT broker service if needed
# volumes:
#   mosquitto_data:
#   mosquitto_log:
#   mosquitto_conf:

# services:
#   mqtt_broker:
#     image: eclipse-mosquitto:2
#     container_name: mqtt_broker
#     ports:
#       - "1883:1883"
#       - "9001:9001" # Optional: for WebSocket support
#     volumes:
#       - ./mosquitto/config:/mosquitto/config:ro
#       - mosquitto_data:/mosquitto/data
#       - mosquitto_log:/mosquitto/log
#     restart: unless-stopped
#     networks:
#       - my_network
